
export interface Movie {
  id: number;
  titulo: string;
  genero: string;
  ano: number;
  minutos: number; // in minutes
  banner: string;
  sinopse: string;
  nota: number; // score from 0 to 10
}
